{% set num_faqs = app.session.get('num_faqs') %}
{% set uname = app.session.get('usr') %} 
{% extends 'AdminPanel/base-side-left-admin-panel.html.twig' %}
{% block content_header %}
    <header class="content-header">
        <!-- header actions -->
        <div class="header-actions pull-right">

            <!-- (recomended: dont change the id value) -->
            <div class="btn-group">
                <a id="users-setting" class="btn btn-icon data-toggle" data-toggle="dropdown" role="button">
                    <i class="icon ion-person"></i>
                </a>
                <ul class="dropdown-menu dropdown-extend pull-right" role="menu">
                    <li class="dropdown-profile">
                        <div class="dp-cover">
                            <img class="img-bg" src="{{ asset('Resources/bootstrap/assets/app/img/cover-blur.jpg') }}" alt="">
                            <a class="img-avatar" href="page_profile.html">
                                <img class="img-circle" src="{{ asset('Resources/bootstrap/assets/app/img/avatar4.png') }}" alt="">
                            </a>
                            <div class="dp-details">Sr/Sra. {{uname}}</div>
                        </div>
                    </li>
                    <li>
                        <div class="row text-center">
                            <a href="{{ path('guias_docentes_app_admin_panel_group_manag')}}" class="col-lg-4 col-sm-4 col-sm-4 col-xs-4">{{num_faqs}} <small>FAQS</small></a>
                        </div>
                    </li>
                    <li class="dropdown-footer">
                        <div class="clearfix">
                            <a href="{{ path('logout') }}" class="btn btn-sm btn-default pull-right">Cerrar sesión</a>
                            <a href="{{ path ('guias_docentes_app_admin_panel_perfil')}}" class="btn btn-sm btn-default pull-left">Perfil</a>
                        </div>
                    </li><!-- /dropdown-footer -->
                </ul><!-- /dropdown-extend -->
            </div><!-- /btn-group setting -->

            <!-- (recomended: dont change the id value) -->
            {#<a id="toggle-aside" class="btn btn-icon" role="button"><i class="icon ion-navicon-round"></i></a>#}
        </div><!-- /header actions -->

        <!-- header actions -->
        <div class="header-actions pull-left">
            <!-- (recomended: dont change the id value) -->
            <button id="toggle-content" class="btn btn-icon" type="button"><i class="icon ion-navicon-round"></i></button>
            <!-- (recomended: dont change the id value) -->
            {#<button id="toggle-search" class="btn btn-icon" type="button"><i class="icon ion-search"></i></button>#}
        </div><!-- /header actions -->

        <!-- your Awesome App title -->
        <h1 class="content-title">Escritorio</h1>
    </header><!-- /side left -->
{% endblock %}